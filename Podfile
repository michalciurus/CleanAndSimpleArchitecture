# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'NetworkAPI' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  pod 'Alamofire', '~> 4.7'

end

target 'ParticleBox' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ParticleBox

  target 'ParticleBoxTests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'SharedTools' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SharedTools

  target 'SharedToolsTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
